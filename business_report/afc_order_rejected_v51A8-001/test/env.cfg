
TEST_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
export DECIDESPACE="BKKTSC-1"

export LAYOUT_FILE="${TEST_DIR}/../layout/${RPT_NAME}.jasper"
export LOG_FILE="${TEST_DIR}/log/${LOG_FILE_NAME}"
export DB_FILE="${TEST_DIR}/db/$DB_FILE_NAME"

export PDF_FILE="${TEST_DIR}/out/${PDF_FILE_NAME}"
export JPG_FILE="${TEST_DIR}/out/${JPG_FILE_NAME}"
export CSV_FILE="${TEST_DIR}/out/${CSV_FILE_NAME}"
export TXT_FILE="${TEST_DIR}/out/${TXT_FILE_NAME}"
export HTML_FILE="${TEST_DIR}/out/${HTML_FILE_NAME}"
export XML_FILE="${TEST_DIR}/out/${XML_FILE_NAME}"
export EXCEL_FILE="${TEST_DIR}/out/${EXCEL_FILE_NAME}"
export JCSV_FILE="${TEST_DIR}/out/${JCSV_FILE_NAME}"
export JPSV_FILE="${TEST_DIR}/out/${JPSV_FILE_NAME}"
export JTXT_FILE="${TEST_DIR}/out/${TXT_FILE_NAME}"

